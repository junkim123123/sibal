# Sanity API 토큰 저장 완료

## ✅ 완료된 작업

Sanity API 토큰이 `web/.env.local` 파일에 성공적으로 저장되었습니다.

## 저장된 환경 변수

```env
NEXT_PUBLIC_SANITY_PROJECT_ID="m4g1dr67"
NEXT_PUBLIC_SANITY_DATASET="production"
SANITY_API_TOKEN=your-api-token-here
```

⚠️ **보안**: 실제 API 토큰은 `.env.local` 파일에만 저장하고, 이 파일에는 포함하지 마세요.

## 토큰 정보

- **이름**: Nexi
- **권한**: Contributor
- **생성일**: 방금 생성됨

## 참고사항

⚠️ **보안 주의사항**:
- 이 토큰은 절대 공개하지 마세요
- `.env.local` 파일은 `.gitignore`에 포함되어 있어 Git에 커밋되지 않습니다
- 토큰이 유출되면 Sanity 대시보드에서 즉시 삭제하세요

## 다음 단계

1. 개발 서버 재시작 (환경 변수 적용을 위해)
2. Sanity Studio 실행
3. API를 통한 데이터 읽기/쓰기 가능

